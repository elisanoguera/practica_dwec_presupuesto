name: 01 - Fundamentos JS I - Tests

on:
  push:
    branches: [ main, practica-1-AdrianOlmedo ]
    paths:
      - 'test/01_**.cy.js'
      - 'js/gestionPresupuesto.js'
      - 'package.json'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm ci
      - run: npm run test1