name: 02 - Fundamentos JS II - Tests

on:
  push:
    branches: [ main, practica-1-AdrianOlmedo ]
    paths:
      - 'test/02_**.cy.js'
      - 'js/gestionPresupuesto.js'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm ci
      - run: npm run test2